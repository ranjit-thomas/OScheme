(*
 *	Parser.ml 
 *
 *	Contains implementation of all functions and attribute of Parser module.
 *
 *)

(* TODO: Consider seeing if I can migrate the S-Expression stuff to its own class. *)

type s_expression = string 
	(*
	| Symbol of S_Symbol
	| List of S_List
	| Value of S_Value
	| Definition of S_Definition
	| Conditional of S_Conditional
	| Func-Call of S_Func_Call
	*)

let tokens_to_s_expr some_list = "dog"
